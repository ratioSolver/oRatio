name: CMake

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        # os: [ubuntu-latest, windows-latest, macos-latest]
        os: [ubuntu-latest, windows-latest]
        build_type: [Debug, Release]
        temporal_network: [LA, DL]
        graph_pruning: [ON, OFF]
        build_listeners: [ON, OFF]

    env:
      BUILD_TYPE: ${{ matrix.build_type }}
      TEMPORAL_NETWORK_TYPE: ${{ matrix.temporal_network }}
      GRAPH_PRUNING: ${{ matrix.graph_pruning }}
      BUILD_LISTENERS: ${{ matrix.build_listeners }}

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Configure CMake
        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DTEMPORAL_NETWORK_TYPE=${{env.TEMPORAL_NETWORK_TYPE}} -DGRAPH_PRUNING=${{env.GRAPH_PRUNING}} -DBUILD_LISTENERS=${{env.BUILD_LISTENERS}}

      - name: Build
        run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

      - name: Test
        working-directory: ${{github.workspace}}/build
        run: ctest -C ${{env.BUILD_TYPE}} --rerun-failed --output-on-failure
